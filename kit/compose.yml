 

name: devkit

services:
  tinyproxy:
    build:
      context: ..
      dockerfile: kit/proxy/tinyproxy.Dockerfile
    image: devkit/tinyproxy:dev
    volumes:
      - ./proxy/tinyproxy.conf:/etc/tinyproxy/tinyproxy.conf:ro
      - ./proxy/allowlist.txt:/etc/tinyproxy/allowlist.txt:ro
    networks:
      - dev-internal
      - dev-egress
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 8888 || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 10

  dev-agent:
    image: devkit/dev-agent:base
    # Overlays are expected to provide build context or image override and workspace mount
    networks:
      - dev-internal
    environment:
      - HTTP_PROXY=http://tinyproxy:8888
      - HTTPS_PROXY=http://tinyproxy:8888
      - NO_PROXY=localhost,127.0.0.1,tinyproxy
      - SBT_GLOBAL_BASE=/workspace/.devhome/.sbt
    working_dir: /workspace
    stdin_open: true
    tty: true
    user: "1000:1000"
    cap_drop:
      - ALL
    security_opt:
      # Codex 0.130 uses Bubblewrap for the Linux sandbox. Docker's default
      # seccomp profile blocks the namespace syscalls Bubblewrap needs; keep
      # capabilities dropped while allowing the sandbox helper to run.
      - seccomp=unconfined
    volumes:
      - ${WORKSPACE_DIR:-.}:/workspace:rw
      - ivy2-cache:/home/dev/.ivy2
      - coursier-cache:/home/dev/.cache/coursier
      - devkit-auth:/home/dev/.devkit
    depends_on:
      tinyproxy:
        condition: service_healthy
    tmpfs:
      - /tmp

networks:
  dev-internal:
    internal: true
    driver: bridge
  dev-egress:
    driver: bridge

volumes:
  ivy2-cache:
  coursier-cache:
  devkit-auth:
