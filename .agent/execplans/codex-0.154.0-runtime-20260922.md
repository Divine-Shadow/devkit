# Codex 0.154.0 runtime

Update every ordinary Devkit overlay and the architecture-specific bundle
derivations from 0.153.4 to 0.154.0 using the official immutable archive
hashes. Acceptance requires the complete flake gate, both-system evaluation,
and explicit ordinary runtime bundle/tool/shell realization before publication.

Preserve all model, lifecycle, reset, auth, and workspace semantics. This plan
owns only runtime identity and package bytes.
